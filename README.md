# PM Dashboard 📊

Красивый дашборд для работы с Google Sheets данными продакт-менеджеров.

## 🚀 Возможности

- **Google Sheets интеграция** — подключение к любой таблице по ID
- **Material Design 3** — современный и красивый UI
- **AI Помощник** — встроенный чат для анализа данных
- **Responsive дизайн** — работает на всех устройствах
- **Real-time данные** — автоматическое обновление

## 📦 Установка

### Предварительные требования

- Node.js 18+
- npm или yarn

### Установка зависимостей

```bash
npm run install:all
```

Эта команда установит зависимости для корневого проекта, сервера и клиента.

## 🏃‍♂️ Запуск

### Development режим

```bash
npm run dev
```

Это запустит:
- Backend сервер на `http://localhost:3001`
- Frontend (Vite) на `http://localhost:5173`

### Отдельный запуск

```bash
# Только сервер
npm run server

# Только клиент
npm run client
```

## 🔧 Настройка Google Sheets

1. **Настройка Service Account**
   - Файл `nir-center-dashboard-31d551f58f41.json` уже добавлен
   - Email сервисного аккаунта: `nir-center-bot@nir-center-dashboard.iam.gserviceaccount.com`

2. **Дать доступ к таблице**
   - Откройте вашу Google таблицу
   - Нажмите "Поделиться"
   - Добавьте email сервисного аккаунта с правами "Читатель"

3. **Получить ID таблицы**
   - ID находится в URL таблицы после `/d/`
   - Пример: `https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID/edit`

4. **Подключиться в дашборде**
   - Вставьте ID в поле на главной странице
   - Нажмите "Подключить"

## 📁 Структура проекта

```
dashboard/
├── client/                    # React фронтенд
│   ├── src/
│   │   ├── components/        # UI компоненты
│   │   │   ├── ai/           # AI помощник
│   │   │   ├── dashboard/    # Компоненты дашборда
│   │   │   └── layout/       # Layout компоненты
│   │   ├── pages/            # Страницы приложения
│   │   ├── theme.ts          # Material UI тема
│   │   └── App.tsx           # Главный компонент
│   └── package.json
│
├── server/                    # Node.js бэкенд
│   ├── src/
│   │   ├── routes/           # API роуты
│   │   ├── services/         # Сервисы (Google Sheets)
│   │   └── index.js          # Точка входа
│   └── package.json
│
└── package.json               # Корневой package.json
```

## 🎨 Технологии

### Frontend
- **React 18** + TypeScript
- **Material UI 5** (Material Design 3 тема)
- **Framer Motion** — анимации
- **React Router** — роутинг
- **Axios** — HTTP клиент
- **Vite** — сборщик

### Backend
- **Node.js** + Express
- **Google APIs** — работа с Sheets API
- **CORS** — кросс-доменные запросы

## 🔌 API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/health` | Проверка состояния сервера |
| GET | `/api/sheets/info/:id` | Информация о таблице |
| GET | `/api/sheets/data/:id?range=Sheet1!A1:Z` | Сырые данные из диапазона |
| GET | `/api/sheets/objects/:id/:sheetName` | Данные как объекты (с заголовками) |
| GET | `/api/sheets/stats/:id/:sheetName` | Статистика по листу |

## 🤖 AI Помощник

AI помощник находится в правой панели и может:
- Делать резюме данных
- Находить тренды и аномалии
- Отвечать на вопросы о данных

> 💡 Для полноценной работы AI добавьте OpenAI API ключ в настройках

## 📝 TODO

- [ ] Интеграция с OpenAI для реального AI анализа
- [ ] Экспорт данных в PDF/Excel
- [ ] Графики с @mui/x-charts
- [ ] Тёмная тема
- [ ] Сохранение избранных таблиц
- [ ] Уведомления об изменениях

## 📄 Лицензия

MIT




